# Redis 설정 (Docker Compose 사용 시 자동 설정됨)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Supabase 설정
# 새로운 API 키 형식 사용 (기존 anon/service_role JWT 키 대신)
# - Publishable Key: 클라이언트용, RLS 정책 적용 (sb_publishable_...)
# - Secret Key: 서버 전용, RLS 우회 - 절대 공개 금지 (sb_secret_...)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_publishable_key
SUPABASE_SECRET_KEY=sb_secret_your_secret_key

# Google Gemini API (자막 번역/언어 분석용)
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI API (임베딩 생성용)
OPENAI_API_KEY=your_openai_api_key_here
COHERE_API_KEY=your_api_key_here

# 클라우드 스토리지 설정 (Cloudflare R2 또는 S3) - Supabase Storage로 대체 가능
# 현재 R2 적용
# 1,000,000 Class A operations per month + $4.50 per additional 1,000,000 operations
# 10,000,000 Class B operations per month + $0.36 per additional 1,000,000 operations
# 10 GB-month storage + $0.015 per additional 1 GB-month of storage
R2_ACCOUNT_ID=your_account_id
R2_ACCESS_KEY_ID=your_access_key_id
R2_SECRET_ACCESS_KEY=your_secret_access_key
# STORAGE_REGION=auto
R2_BUCKET_NAME=karaoke-assets
R2_PUBLIC_URL=https://your-account-id.r2.cloudflarestorage.com

# 임시 파일 디렉토리
TEMP_DIR=/tmp/karaoke-gen
